{
  "entities": {
    "AppConfig": {
      "title": "App Configuration",
      "description": "Global settings for the application managed by the admin.",
      "type": "object",
      "properties": {
        "registrationDeadline": {
          "type": "string",
          "format": "date-time",
          "description": "The ISO date string for when registration ends."
        },
        "isRegistrationEnabled": {
          "type": "boolean",
          "description": "Toggle to manually enable or disable registration."
        }
      },
      "required": ["registrationDeadline", "isRegistrationEnabled"]
    },
    "UserRegistration": {
      "title": "User Registration",
      "description": "Participant details for the ICT Olympiad.",
      "type": "object",
      "properties": {
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "institution": { "type": "string" },
        "category": { "type": "string" },
        "tshirtSize": { "type": "string" },
        "registeredAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": []
  },
  "firestore": {
    "/settings/olympiad": {
      "schema": "#/backend/entities/AppConfig",
      "description": "Dynamic configuration for the Olympiad registration."
    },
    "/registrations/{regId}": {
      "schema": "#/backend/entities/UserRegistration",
      "description": "Stores participant registration data."
    }
  }
}
